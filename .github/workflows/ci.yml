name: CI Pipeline

on:
  push:
    branches: "**"
    tags: ["v*"]
  pull_request:
    branches: "**"

jobs:
  Quality-Checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.9'
          cache: 'true'

      - name: Sanity check
        run: go vet ./...

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: --timeout=5m

  Integrity-Tests:
    needs: Quality-Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.9'
          cache: 'true'

      - name: Check go.mod is tidy
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Check for vulnerabilities
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: Run tests
        run: |
          go test -race -coverprofile=coverage.out -covermode=atomic ./...
          go tool cover -func=coverage.out
      - uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: coverage.out

  Build-Release:
    if: github.event_name == 'push'
    needs: Integrity-Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [windows, linux, darwin]
        arch: [amd64, arm64]

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.9'
          cache: 'true'

      - name: Build
        run: |
          GOOS=${{ matrix.os }} GOARCH=${{ matrix.arch }} go build -o bin/seniorlens-${{ matrix.os }}-${{ matrix.arch }} ./cmd/seniorlens
      - uses: actions/upload-artifact@v7
        with:
          name: seniorlens-${{ matrix.os }}-${{ matrix.arch }}
          path: bin/
          retention-days: 7

  open_pull_request:
    if: github.ref != 'refs/heads/main' && github.event_name == 'push' && github.ref != 'refs/heads/stable'
    needs:
      - Build-Release
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get body content
        id: pr_body
        run: |
          echo "COMMITS_LIST<<EOF" >> $GITHUB_ENV
          echo "### List of changements: " >> $GITHUB_ENV
          git log origin/main..HEAD --oneline --pretty=format:"- %s (%h)" >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Create PR ${{ github.ref_name }} -> main
        run: |
          gh pr create \
            --title "merge: ${{ github.ref_name }} -> main" \
            --body "$COMMITS_LIST" \
            --base main \
            --head ${{ github.ref_name }} || echo "PR already exists or could not be created"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  GitHub-Release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: Build-Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true
          pattern: seniorlens-*

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**
          generate_release_notes: true

  # Deploy:
  #   if: startsWith(github.ref, 'refs/tags/v')
  #   needs: [Build-Release, GitHub-Release]
  #   runs-on: ubuntu-latest
  #   permissions:
  #     packages: write
  #     contents: read

  #   steps:
  #     - uses: actions/checkout@v6
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v4

  #     - name: Log in to GHCR
  #       uses: docker/login-action@v4
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Extract metadata
  #       id: meta
  #       uses: docker/metadata-action@v6
  #       with:
  #         images: ghcr.io/${{ github.repository }}
  #         tags: |
  #           type=sha,format=long
  #           type=semver,pattern={{version}}
  #           type=semver,pattern={{major}}.{{minor}}
  #           type=raw,value=stable

  #     - name: Build and push
  #       uses: docker/build-push-action@v7
  #       with:
  #         context: .
  #         file: .github/Dockerfile
  #         push: true
  #         tags: ${{ steps.meta.outputs.tags }}
  #         labels: ${{ steps.meta.outputs.labels }}
  #         cache-from: type=gha,scope=app-cache
  #         cache-to: type=gha,mode=max,scope=app-cache
  #         provenance: false
  #         sbom: false